import React  from 'react';

var List = () => {
    return(

                <button onClick={XMLHttpRequestUpload}>
               Upload File
                </button>
        
    );
}

export default List;